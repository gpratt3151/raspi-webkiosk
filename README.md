# raspi-webkiosk
A web kiosk for the Raspberry Pi Zero W

# Burn Image
Use `Etcher` to burn raspian stretch lite
Execute pre.sh

# After Initial boot
Execute:
```bash
sudo apt-get install -y git-core && git clone https://github.com/gpratt3151/raspi-webkiosk.git
```
